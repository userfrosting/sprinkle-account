---
email:
  validators:
    required:
      label: "&EMAIL"
      message: VALIDATE.REQUIRED
code:
  validators:
    required:
      label: "&ACCOUNT.VERIFICATION.CODE"
      message: VALIDATE.REQUIRED
